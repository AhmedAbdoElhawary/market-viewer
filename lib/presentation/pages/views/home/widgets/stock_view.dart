part of '../home_page.dart';

class _StockTabBarView extends StatelessWidget {
  const _StockTabBarView();

  @override
  Widget build(BuildContext context) {
    return const Center(child: RegularText("First one"));
  }
}
